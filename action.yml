name: "AWS Deploy Action"
description: "Build distribution using npm, syncs dist folder to an AWS S3 bucket and invalidates a AWS CloudFront distribution."
author: mirceapreotu
runs:
  using: docker
  image: Dockerfile
branding:
  icon: upload-cloud
  color: green